<?php

global $G_INPUT;

require_once("grn/smarty.csp");
$t = new GRN_Smarty;

$mygroup_id = null;
if (array_key_exists('mgid', $G_INPUT)) {
    $mygroup_id = $G_INPUT['mgid'];
}

global $G_container_base;
$uum =& $G_container_base->getInstance('uum');
$user =& $uum->getLoginUser();

require_once('address/mygroup_logic.csp');
$mygroup = GRN_Address_MyGroup_Logic::getInstance();
$group_info = $mygroup->getMyGroupInfo($user, $mygroup_id);

if ($group_info === false) {
    cb_throw_error(E_GRN_ADDRESS_MYGROUP_NOT_FOUND);
}

$t->assign('mygroup_id', $mygroup_id);
$t->assign('group_name', $group_info['name']);
$t->assign('memo', $group_info['memo']);

require('SmartyValidate.class.php');
SmartyValidate::connect($t);
SmartyValidate::register_form(cb_get_pagename(), true);

// page title
$page_title = grn_get_current_page_display_name();
$t->assign('page_title', $page_title);

// page navigation
$page_info = [
    'mygroup_list'   => ['mgid' => $mygroup_id],
    'mygroup_view'   => ['mgid' => $mygroup_id],
    'mygroup_modify' => null
];
require_once('grn/controller.csp');
$controller_util = new GRN_ControllerUtil();
$site_position = $controller_util->makeSitePosition('address/', $page_info);
$t->assign('site_position', $site_position);

$t->display(cb_get_pagename() . ".tpl");


