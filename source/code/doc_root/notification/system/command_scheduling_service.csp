<?php

use grn\notification\screen\CommandSchedulingService;

global $G_INPUT;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    $screen = new CommandSchedulingService($G_INPUT);
    $screen->post();
}

cb_redirect('system/application_list', ['app_id' => 'notification']);

