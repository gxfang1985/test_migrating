<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    require_once('../_access_util.csp');

    // access information
    require_once('cabinet/access.csp');
    $am = GRN_Cabinet_AccessManager::getInstance();

    // 現在の設定をすべてクリアする
    $am->deleteTargets($node_row, null);
}

