<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    if ( ! ($file_id = @ $G_INPUT['fid'])) {
        cb_throw_error(E_GRN_CABINET_FILE_NOT_FOUND);
    }

    $G_cabinet->deleteFile($G_cabinet_login, $file_id, @ $G_INPUT['hid']);
}
