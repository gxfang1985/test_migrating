<?php
if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    require_once('../_command_unlock.csp');
    cb_redirect('cabinet/system/view',
        ['hid' => @$G_INPUT['hid'], 'fid' => @$G_INPUT['fid']]);
}

