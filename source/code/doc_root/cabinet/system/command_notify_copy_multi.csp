<?php
if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $classification = "notify";
    include('../_command_copy_multi.csp');
    cb_redirect('cabinet/system/notify_list', ['nid' => $G_INPUT['hid']]);
}
