<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    require_once('../_command_access_delete_multi.csp');

    cb_redirect('cabinet/system/access_list',
        ['nid' => $G_INPUT['nid'], 'poid' => @$G_INPUT['poid']]);
}

