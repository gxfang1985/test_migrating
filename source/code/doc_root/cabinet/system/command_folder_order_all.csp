<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    require_once('../_command_folder_order.csp');
    cb_redirect('cabinet/system/folder_list', ['hid' => $G_INPUT['hid']]);
}
