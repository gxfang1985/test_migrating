<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    require_once('../_command_folder_delete.csp');
    cb_redirect('cabinet/index', ['hid' => @$parent_id]);
}
