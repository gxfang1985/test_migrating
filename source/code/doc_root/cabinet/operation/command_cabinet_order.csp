<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    require_once('../_command_cabinet_order.csp');

    cb_redirect('cabinet/index', ['hid' => $G_INPUT['hid']]);
}
