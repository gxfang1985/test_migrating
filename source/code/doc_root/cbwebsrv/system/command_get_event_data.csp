<?php

if (is_array(@ $G_INPUT['event_ids'])) {
    $event_ids = [];
    foreach ($G_INPUT['event_ids'] as $id) {
        $event_ids['event_id_' . $id] = $id;
    }

    cb_redirect('cbwebsrv/system/get_event_data2', $event_ids);
} else {
    cb_redirect('cbwebsrv/system/get_event_data');
}


