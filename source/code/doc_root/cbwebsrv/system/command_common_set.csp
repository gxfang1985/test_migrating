<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    require_once('cbwebsrv/system_logic.csp');
    $system = GRN_CBWebSrv_SystemLogic::getInstance();

    // set "use product id" attribute
    if (array_key_exists('send_pid', $G_INPUT)) {
        $row = $system->setUseProductIDAttribute($G_INPUT['send_pid']);
    }
}

cb_redirect('system/application_list', ['app_id' => 'cbwebsrv']);


