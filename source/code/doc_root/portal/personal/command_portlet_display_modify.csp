<?php

// Check HTTP-POST Method
if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    include('_portlet_display_modify_evaluate.csp');

    $portlet_info
        = $my_portal_Logic->getPortletBaseInformation($portlet->get('type'));
    if (isset($portlet_info['command_template'])
        && ! is_null($portlet_info['command_template'])
    ) {
        $command = cb_basedir() . '/code/doc_root/'
                   . $portlet_info['command_template'];
        if (file_exists($command)) {
            include($command);
        }
    }
}
