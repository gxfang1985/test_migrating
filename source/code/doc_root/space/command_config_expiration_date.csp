<?php

use grn\space\screen\CommandConfigExpirationDate;
use grn\space\screen\common\StandardScreenCommon;

global $G_INPUT;

/** @var \grn\space\screen\CommandConfigExpirationDate $screen */
$screen = new StandardScreenCommon(new CommandConfigExpirationDate($G_INPUT));

/** @var \grn\space\data\bean\Space $new_space */
$new_space = $screen->post(null);
cb_redirect("space/config_view", ["spid" => $new_space->getId()]);
