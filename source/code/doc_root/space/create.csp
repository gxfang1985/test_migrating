<?php

use grn\space\screen\CreateSpace;
use grn\space\screen\common\StandardScreenCommon;
use grn\grn\access\service\AppAccess;

global $G_INPUT;
if (array_key_exists('sUID', $G_INPUT)) {
    $G_INPUT['sUID']
        = AppAccess::filterUserIdsInternalAccessByAppId($G_INPUT['sUID'],
        "space");
}
$screen = new StandardScreenCommon(new CreateSpace($G_INPUT));
echo $screen->fetch();


