<?php

use grn\space\screen\SystemMoveMultiSpaces;

global $G_INPUT;
$systemMoveMultiSpaces = new SystemMoveMultiSpaces($G_INPUT);
$result = $systemMoveMultiSpaces->post($G_INPUT);
if ($result == false) {
    echo $systemMoveMultiSpaces->fetch();
} else {
    cb_redirect($result[0], $result[1]);
}
