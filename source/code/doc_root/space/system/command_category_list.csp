<?php

use grn\space\screen\CategorySpaceList;
use grn\space\screen\SystemCategoryList;

global $G_INPUT;

$screen = new SystemCategoryList($G_INPUT);

$data = $screen->post($G_INPUT);
if (is_array($data)) {
    cb_redirect($data[0], $data[1]);
}


