<?php

use grn\space\screen\CommandCreateSpace;
use grn\space\screen\common\StandardScreenCommon;

global $G_INPUT;

$screen = new StandardScreenCommon(new CommandCreateSpace($G_INPUT));
$newSpace = $screen->post(null);

/** @var \grn\space\data\bean\Space $newSpace */
cb_redirect("space/top", ["spid" => $newSpace->getId()]);
