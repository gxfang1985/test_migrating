<?php

use grn\space\screen\mobile\application\todo\TodoIndex;
use grn\grn\JSONResponse;

global $G_INPUT;
$todo_component = new TodoIndex($G_INPUT);

global $G_state_set;
$G_state_set->set('error_page_type', 'json');

$todo_component->setIsFromAjax(true);
$json = JSONResponse::create();
$json->response([
    'list'   => $todo_component->fetch(),
    'offset' => $todo_component->getNewOffset()
]);

