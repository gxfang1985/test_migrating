<?php

use grn\space\screen\mobile\SpaceMemberList;
use grn\grn\JSONResponse;

global $G_INPUT;
$memberList = new SpaceMemberList($G_INPUT);

global $G_state_set;
$G_state_set->set('error_page_type', 'json');

$memberList->setIsFromAjax(true);
$json = JSONResponse::create();
$json->response([
    'list'   => $memberList->fetch(),
    'offset' => $memberList->getNewOffset()
]);

