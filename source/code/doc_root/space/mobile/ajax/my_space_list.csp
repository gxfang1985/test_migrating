<?php

use grn\space\screen\mobile\MySpaceList;
use grn\grn\JSONResponse;

global $G_INPUT;
$my_space_list = new MySpaceList($G_INPUT);

global $G_state_set;
$G_state_set->set('error_page_type', 'json');

$json = JSONResponse::create();
$json->response([
    'list'   => $my_space_list->fetchForAjax(),
    'offset' => $my_space_list->getNewOffset()
]);


