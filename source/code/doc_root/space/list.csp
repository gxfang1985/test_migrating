<?php

use grn\space\screen\RootSpaceList;
use grn\space\screen\CategorySpaceList;
use grn\space\screen\common\SpaceListScreenCommon;

global $G_INPUT;

$innerScreen = array_key_exists("top", $G_INPUT) ? new RootSpaceList($G_INPUT)
    : new CategorySpaceList($G_INPUT);
$screen = new SpaceListScreenCommon($innerScreen);
echo $screen->fetch();
