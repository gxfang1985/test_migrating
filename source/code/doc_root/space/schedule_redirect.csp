<?php

use grn\space\screen\ScheduleRedirect;
use grn\space\screen\common\StandardScreenCommon;

global $G_INPUT;

$screen = new StandardScreenCommon(new ScheduleRedirect($G_INPUT));
$newSpace = $screen->post(null);

cb_redirect("schedule/group_day", [
    "event" => "selected",
    "gid"   => "selected",
]);
