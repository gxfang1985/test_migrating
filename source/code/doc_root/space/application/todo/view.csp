<?php

require_once("space/screen/application/todo/TodoView.csp");
require_once("space/screen/common/ApplicationScreenCommon.csp");

global $G_INPUT;

use grn\space\screen\application\todo\TodoView;
use grn\space\screen\common\ApplicationScreenCommon;

if (array_key_exists('nid', $G_INPUT) || array_key_exists('nhid', $G_INPUT)
    || array_key_exists('fnid', $G_INPUT)
) {
    $G_INPUT['no_header'] = false;
}
$screen = new ApplicationScreenCommon(new TodoView($G_INPUT));
echo $screen->fetch();

