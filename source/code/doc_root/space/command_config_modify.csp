<?php

use grn\space\screen\CommandConfigModify;
use grn\space\screen\common\StandardScreenCommon;

global $G_INPUT;

$screen = new StandardScreenCommon(new CommandConfigModify($G_INPUT));
$newSpace = $screen->post(null);

cb_redirect("space/config_view", ["spid" => $newSpace->getId()]);
