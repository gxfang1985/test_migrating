<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    $category_id = @ $G_INPUT['cid'];
    $eid = $G_INPUT['eid'];

    foreach ($eid as $aid) {
        $article =& $G_bulletin->getArticle($G_bulletin_login, $aid);
        if ($article !== false) {
            $article->delete($G_bulletin_login);
        }
    }
}
