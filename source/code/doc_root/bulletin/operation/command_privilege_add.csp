<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    require_once('fw/session_manager.csp');
    $session_manager = CB_SessionManager::getInstance();

    $session_name = 'bulletin.operation.privilege_add';
    $session = $session_manager->getSession($session_name);

    $aid = $session->get('target_ids');
    if ( ! is_array($aid)) {
        cb_redirect('bulletin/operation/privilege_list',
            ['nid' => cb_at($G_INPUT, 'nid')]);
    }
    require('../_command_privilege_add.csp');

    $params = ['nid' => $node_id];
    cb_redirect('bulletin/operation/privilege_list', $params);
}


