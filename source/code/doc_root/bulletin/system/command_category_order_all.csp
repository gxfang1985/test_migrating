<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    require('../_command_category_order.csp');

    cb_redirect('bulletin/system/category_list',
        ['cid' => @$G_INPUT['cid'], 'sf' => 1]);
}
