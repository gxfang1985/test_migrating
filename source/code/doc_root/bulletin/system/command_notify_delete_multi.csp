<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    require_once('../_command_notify_delete_multi.csp');

    cb_redirect('bulletin/system/notify_list',
        ['nid' => $G_INPUT['nid'], 'poid' => @$G_INPUT['poid']]);
}
