<?php

use grn\mail\screen\mobile\MailDraftModify;
use grn\grn\mobile\MobileScreenCommon;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    $screen = new MobileScreenCommon(new MailDraftModify($G_INPUT));
    $screen->post($G_INPUT);
}
