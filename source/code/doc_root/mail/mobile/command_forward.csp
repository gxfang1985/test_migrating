<?php

use grn\mail\screen\mobile\MailForward;
use grn\grn\mobile\MobileScreenCommon;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    $screen = new MobileScreenCommon(new MailForward($G_INPUT));
    $screen->post($G_INPUT);
}
