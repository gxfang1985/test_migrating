<?php

use grn\mail\screen\MailThreePane;

if (0 == strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST')) {
    global $G_INPUT;

    $three_pane = new MailThreePane($G_INPUT);
    $three_pane->changePane();
    unset($G_INPUT['pane']);

    cb_redirect('mail/index', $G_INPUT);
}
