<?php

require_once('_getcontent.csp');

$t->assign('item_id', $item_id);
$t->assign('folder_id', $folder_id);


// インライン表示設定
require_once('grn/ui.csp');
$uim = GRN_UIConfigManager::getInstance();
$system_ui_config =& $uim->getSystemConfig();
$user_ui_config =& $uim->getUserConfig($login);
unset($uim);
$inline = $user_ui_config->getInlineSetting();
$t->assign('inline', $inline);

// page title
$t->assign('page_title', $content->get('title'));

// コピーライト表示OFF
global $G_state_set;
$G_state_set->set('copyright_should_be_written', false);
$G_state_set->set('html_should_be_closed', false);

// 警告をOFF
$t->skipWarning();

// Smarty実行
$doc_name = cb_get_pagename();
$t->display("{$doc_name}.tpl");


