<?php

use grn\schedule\screen\CustomizationGroupModify;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $screen = new CustomizationGroupModify($G_INPUT);
    $screen->post($G_INPUT);
}
