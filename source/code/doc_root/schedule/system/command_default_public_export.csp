<?php

use grn\schedule\ScheduleDefaultPublicLogic;

if (strcasecmp(cb_at($_SERVER, "REQUEST_METHOD"), "POST") == 0) {
    global $G_INPUT;
    echo ScheduleDefaultPublicLogic::getInstance()->exportToCSV($G_INPUT);
}
