<?php

use grn\schedule\screen\CustomizationGroupOrder;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $screen = new CustomizationGroupOrder($G_INPUT);
    $screen->post($G_INPUT);
}
