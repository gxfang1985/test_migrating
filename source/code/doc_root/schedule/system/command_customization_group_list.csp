<?php

use grn\schedule\screen\CustomizationGroupList;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $screen = new CustomizationGroupList($G_INPUT);
    $screen->post($G_INPUT);
}
