<?php

use grn\schedule\screen\CustomizationGroupAdd;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $screen = new CustomizationGroupAdd($G_INPUT);
    $screen->post($G_INPUT);
}
