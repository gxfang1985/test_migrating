<?php

use grn\schedule\screen\CustomizationGroupView;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $screen = new CustomizationGroupView($G_INPUT);
    $screen->post($G_INPUT);
}
