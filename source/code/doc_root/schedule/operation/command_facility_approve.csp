<?php

use grn\schedule\screen;
use grn\schedule\screen\FacilityApproveDetail;

require_once("schedule/facility_system_logic.csp");

if (strcasecmp(@ $_SERVER["REQUEST_METHOD"], "POST") == 0) {
    global $G_INPUT;
    $screen = new FacilityApproveDetail($G_INPUT);
    $screen->post();
}
