<?php

// 予定ニュー
$menus = [];

$temp = $personallogic->getMenus($login);
foreach ($temp as $menu) {
    $menus[] = [$menu, ''];
}
$temp = $systemlogic->getMenus();
if (is_array($temp)) {
    foreach ($temp as $menu) {
        $menus[] = $menu;
    }
}
$t->assign('menus', $menus);


