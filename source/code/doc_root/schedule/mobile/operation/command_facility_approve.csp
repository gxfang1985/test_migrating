<?php

use grn\schedule\screen\mobile\FacilityApproveMobileDetail;

if (strcasecmp(@ $_SERVER["REQUEST_METHOD"], "POST") == 0) {
    global $G_INPUT;
    $screen = new FacilityApproveMobileDetail($G_INPUT);
    $screen->post();
}
