<?php
/**
 * Leave appointment
 *
 * @author: tuyen-truong(Cybozu Vietnam)
 * @date  : 2014/03/06
 */

use grn\schedule\screen\mobile\AppointmentLeave;
use grn\grn\mobile\MobileScreenCommon;

if (strcasecmp(@$_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $screen = new MobileScreenCommon(new AppointmentLeave($G_INPUT));
    $screen->post($G_INPUT);
}
