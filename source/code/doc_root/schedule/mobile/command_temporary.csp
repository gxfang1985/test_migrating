<?php

use grn\schedule\screen\mobile\ScheduleTemporary;
use grn\grn\mobile\MobileScreenCommon;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $screen = new MobileScreenCommon(new ScheduleTemporary($G_INPUT));
    $screen->post($G_INPUT);
}
