<?php

use grn\schedule\screen\mobile\FacilityList;
use grn\grn\JSONResponse;

global $G_INPUT;
$facilityList = new FacilityList($G_INPUT);

global $G_state_set;
$G_state_set->set('error_page_type', 'json');

$json = JSONResponse::create();
$json->response([
    'list'   => $facilityList->facilitySearch(),
    'offset' => $facilityList->getOffset(),
    'total'  => $facilityList->getTotal()
]);

