<?php

use grn\schedule\screen\mobile\SchedulePublicToUserList;
use grn\grn\JSONResponse;

global $G_INPUT;
$user_list = new SchedulePublicToUserList($G_INPUT);

global $G_state_set;
$G_state_set->set('error_page_type', 'json');

$user_list->setIsFromAjax(true);
$json = JSONResponse::create();
$json->response([
    'list'   => $user_list->fetch(),
    'offset' => $user_list->getNewOffset()
]);
