<?php

$logic = &_grn_get_privilege_logic();
$class_name = 'GRN_PrivilegedOrgTree';

global $G_INPUT;
$oid = @$G_INPUT['oid'];
$page_name = @$G_INPUT['page'];
require_once('grn/org_tree.csp');
$util = GRN_OrgTreeUtil::getInstance();
$tree = $util->getTree($page_name, $class_name);
if ( ! $tree->containNode($oid)) {
    $tree = grn_rebuild_tree_group_user($page_name, $class_name, $oid);
}

include('../../grn/_org_json_26.csp');

