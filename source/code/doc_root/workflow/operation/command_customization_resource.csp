<?php

use grn\workflow\screen\WorkflowCustomizationResourceScreen;

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;
    $screen = new WorkflowCustomizationResourceScreen($G_INPUT);
    echo $screen->post($G_INPUT);
}
