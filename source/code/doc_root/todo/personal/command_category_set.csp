<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    require('../_command_category_set.csp');

    cb_redirect('personal/application_list', ['app_id' => 'todo']);
}
