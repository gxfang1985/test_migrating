<?php

global $G_INPUT;

//Instantiate an Smarty object
require_once("grn/smarty.csp");
$t = new GRN_Smarty;

//Get Genaral Configuration
require_once('link/controller_util.csp');
$link_util = GRN_Link_Link_Controller_Utility::getInstance();
$link_util->setType('system');
$config_list = $link_util->getGeneralConfig();

//Create Popup Set for View
if (array_key_exists('popup_set', $config_list)) {
    $popup_set_for_view = $config_list['popup_set'];
} else {
    $popup_set_for_view = 0;
}

//popup_set 0:アイコン 1:URL
$t->assign('popup_set', $popup_set_for_view);

//page title
$page_title = grn_get_current_page_display_name();
$t->assign('page_title', $page_title);

//site position
$t->assign('site_position', [
        ['page' => '', 'name' => $page_title]
    ]
);

//Display Smarty Template
$doc_name = cb_get_pagename();
$t->display("{$doc_name}.tpl");


