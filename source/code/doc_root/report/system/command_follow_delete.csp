<?php

if (strcasecmp(@ $_SERVER['REQUEST_METHOD'], 'POST') == 0) {
    global $G_INPUT;

    $system_page = true;

    require_once('../_command_follow_delete.csp');
    cb_redirect('report/system/report_view', [
        'cid' => @$G_INPUT['cid'],
        'fid' => @$G_INPUT['fid'],
        'rid' => $G_INPUT['rid']
    ]);
}

