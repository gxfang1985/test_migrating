<?php

use grn\report\screen\mobile\ReportAttendeeList;
use grn\grn\JSONResponse;

global $G_INPUT;
$attendee_list = new ReportAttendeeList($G_INPUT);

global $G_state_set;
$G_state_set->set('error_page_type', 'json');

$attendee_list->setIsFromAjax(true);
$json = JSONResponse::create();
$json->response([
    'list'   => $attendee_list->fetch(),
    'offset' => $attendee_list->getNewOffset()
]);
