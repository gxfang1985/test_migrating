<?php

use grn\report\screen\mobile\ReportWatchersList;
use grn\grn\JSONResponse;

global $G_INPUT;
$watchers_list = new ReportWatchersList($G_INPUT);

global $G_state_set;
$G_state_set->set('error_page_type', 'json');

$watchers_list->setIsFromAjax(true);
$json = JSONResponse::create();
$json->response([
    'list'   => $watchers_list->fetch(),
    'offset' => $watchers_list->getNewOffset()
]);
