<?php

use grn\fts\mail\MailFileParser;
use grn\fts\IndexerLogger;

if ( ! defined('ON_FOREST')) {
    echo "This command is available on FOREST only!!\n";
    echo "Exit function.\n";
    cb_safe_exit();
}

global $G_INPUT;

$mailFileParser = new MailFileParser($G_INPUT, new IndexerLogger($G_INPUT));
$mailFileParser->parseFileNotRelatedMails();
