<?php

use grn\fts\mail\CommandMailFileParser;
use grn\fts\IndexerLogger;

if ( ! defined('ON_FOREST')) {
    echo "This command is available on FOREST only!!\n";
    echo "Exit function.\n";
    cb_safe_exit();
}

global $G_INPUT;
$commandMailFileParser = new CommandMailFileParser($G_INPUT,
    new IndexerLogger($G_INPUT));
$commandMailFileParser->run();
