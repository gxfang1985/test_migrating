*''date_format'' 修正子は使用禁止とする。
内部で strftime を使用しているため。
代わりに、[[cb_date_format|doc:pg/i18n/datetime]]（[[デモ実行|run:developer/demo_i18n_message]]）が提供されているので、そちらを利用せよ。

*''truncate'' 修正子は使用禁止とする。
マルチバイト対応していないため。
代わりに、''cb_mb_truncate'' が提供されているので、そちらを利用せよ。
